# Inputs
input dirInput {
  start: [Float!]!
  range: Int!
  filters: [String]
  type: String!
}

type LocationResp {
  id: String!
  route: Directions!
}

type Query {
  getLocation(input: dirInput!): LocationResp!
}
