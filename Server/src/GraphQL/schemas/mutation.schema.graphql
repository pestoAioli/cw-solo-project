input InputPoint {
  latitude: Float!
  longitude: Float!
}

input destInput {
  userID: String!
  locationID: String!
  date: String!
  polilyne: [InputPoint!]!
  origin: InputPoint!
}

type Mutation {
  addVisitedDestination(input: destInput!): String!
}
